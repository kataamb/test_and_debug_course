import React from 'react';
import Login from '../../components/Login/Login'; // Импортируем компонент формы

export const LoginPage: React.FC = () => {
  return (
    <div >
      <Login />
    </div>
  );
};

export default LoginPage;
